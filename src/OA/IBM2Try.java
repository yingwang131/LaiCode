package OA;

import java.util.List;

public class IBM2Try {
    /*
    //question 1:
    public static String getShiftString(String s, int leftShifts, int rightShifts){
        int size = s.length();
        leftShifts = leftShifts % size;
        if (leftShifts > 0){
            char[] array = new char[size];
            for (int i =0; i < size; i++){
                if (i < leftShifts){
                    array[i-leftShifts+size] = s.charAt(i);
                 }else{
                    array[i - leftShifts] = s.charAt(i);
                }
            }
            s = new String(array);
        }
        if (rightShifts > 0){
            char[] array = new char[size];
            for (int i =0; i < size; i++){
                if (i + rightShifts < size){
                    array[i + rightShifts] = s.charAt(i);
                }else{
                    array[(i + rightShifts) % size] = s.charAt(i);
                }
            }
            s = new String(array);
        }
        return s;
    }
    public static void main(String[] args) {
        System.out.println(getShiftString("abcdefg", 2, 4));

    }

     */

    //question 2:
//    public static int minMoves(List<Integer> arr){
//
//    }

}
